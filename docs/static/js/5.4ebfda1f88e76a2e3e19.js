(window["webpackJsonp"] = window["webpackJsonp"] || []).push([[5],{

/***/ "/sSO":
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _interopRequireWildcard = __webpack_require__("284h");

var _interopRequireDefault = __webpack_require__("TqRt");

Object.defineProperty(exports, "__esModule", {
  value: true
});
Object.defineProperty(exports, "getStyle", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.getStyle;
  }
});
Object.defineProperty(exports, "setStyle", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.setStyle;
  }
});
Object.defineProperty(exports, "hasClass", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.hasClass;
  }
});
Object.defineProperty(exports, "addClass", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.addClass;
  }
});
Object.defineProperty(exports, "removeClass", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.removeClass;
  }
});
Object.defineProperty(exports, "on", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.on;
  }
});
Object.defineProperty(exports, "off", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.off;
  }
});
Object.defineProperty(exports, "once", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.once;
  }
});
Object.defineProperty(exports, "dataTypeTest", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.dataTypeTest;
  }
});
Object.defineProperty(exports, "deepCopyObject", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.deepCopyObject;
  }
});
Object.defineProperty(exports, "deepCopyArray", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.deepCopyArray;
  }
});
Object.defineProperty(exports, "deepCopy", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.deepCopy;
  }
});
Object.defineProperty(exports, "formatDate", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.formatDate;
  }
});
Object.defineProperty(exports, "GenNonDuplicateID", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.GenNonDuplicateID;
  }
});
Object.defineProperty(exports, "arrayFilterBy", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.arrayFilterBy;
  }
});
Object.defineProperty(exports, "EetoString", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.EetoString;
  }
});
Object.defineProperty(exports, "onNoRepeatEvent", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.onNoRepeatEvent;
  }
});
Object.defineProperty(exports, "filterQuery", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.filterQuery;
  }
});
Object.defineProperty(exports, "parseQueryString", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.parseQueryString;
  }
});
Object.defineProperty(exports, "formatterMapKey", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.formatterMapKey;
  }
});
Object.defineProperty(exports, "isUrl", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.isUrl;
  }
});
Object.defineProperty(exports, "checkDevices", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.checkDevices;
  }
});
Object.defineProperty(exports, "loadFileList", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.loadFileList;
  }
});
Object.defineProperty(exports, "dataType", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.dataType;
  }
});
Object.defineProperty(exports, "firstWordToUpperCase", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.firstWordToUpperCase;
  }
});
Object.defineProperty(exports, "removeItemFromTree", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.removeItemFromTree;
  }
});
Object.defineProperty(exports, "replaceItemFromTree", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.replaceItemFromTree;
  }
});
Object.defineProperty(exports, "insertBeforeItemFromTree", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.insertBeforeItemFromTree;
  }
});
Object.defineProperty(exports, "insertAfterItemFromTree", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.insertAfterItemFromTree;
  }
});
Object.defineProperty(exports, "pushItemToTree", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.pushItemToTree;
  }
});
Object.defineProperty(exports, "itemsFromTree", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.itemsFromTree;
  }
});
Object.defineProperty(exports, "unshiftItemToTree", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.unshiftItemToTree;
  }
});
Object.defineProperty(exports, "isWhiteColor", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.isWhiteColor;
  }
});
Object.defineProperty(exports, "turnMapKeys", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.turnMapKeys;
  }
});
Object.defineProperty(exports, "turnLabelOrValue", {
  enumerable: true,
  get: function get() {
    return _zerodZtool.turnLabelOrValue;
  }
});
exports.default = exports.zTool = exports.mergeConfig = exports.IntroInstance = exports.listenDivSizeChange = exports.scrollDisableWheel = exports.BuildScroll = exports.IScrollInstance = exports.httpAjax = void 0;

var _defineProperty2 = _interopRequireDefault(__webpack_require__("lSNA"));

var _iscroll = _interopRequireDefault(__webpack_require__("iQKG"));

var _httpAjax = _interopRequireDefault(__webpack_require__("IvKo"));

var _debounce = _interopRequireDefault(__webpack_require__("sEfC"));

var _ResizeSensor = _interopRequireDefault(__webpack_require__("mtxb"));

var _merge = _interopRequireDefault(__webpack_require__("QkVN"));

var _zerodZtool = _interopRequireWildcard(__webpack_require__("mQv5"));

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(source, true).forEach(function (key) { (0, _defineProperty2.default)(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(source).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

var httpAjax = _httpAjax.default;
exports.httpAjax = httpAjax;

//
function isChrome() {
  var userAgent = window.navigator.userAgent;
  return userAgent.indexOf("Chrome") > -1;
} //生成IScroll 滚动条实例


var IScrollInstance = function IScrollInstance(el, opt) {
  var defaultopt = Object.assign({
    disableMouse: true,
    disablePointer: true,
    disableTouch: false,
    bounce: false,
    //当滚动条符合边界时，它会执行一个小的反弹动画
    click: false,
    //要覆盖原生滚动，iScroll必须禁止某些默认浏览器行为，例如鼠标单击。如果您希望应用程序响应click事件，则必须将此选项显式设置为true。请注意，建议使用自定义tap事件
    scrollX: false,
    //启用横向滚动
    scrollY: true,
    //启用纵向滚动
    mouseWheel: true,
    //启用滚轮
    preventDefault: false,
    bindToWrapper: false,
    //的move事件通常是结合于文件而不是滚动容器。当您将光标/手指移出包装器时，滚动会继续进行。这通常是您想要的，但您也可以将move事件绑定到包装器本身。一旦指针离开容器，滚动就会停止。
    eventPassthrough: false,
    //有时您希望保留原生垂直滚动但能够添加水平iScroll（可能是旋转木马）。将此设置为true
    scrollbars: false,
    //显示滚动条，自定义滚动条样式：scrollbars选项'custom'
    interactiveScrollbars: true,
    //滚动条可拖动
    shrinkScrollbars: "scale",
    //当滚动到边界之外时，滚动条会缩小一小部分。'clip'和'scale'
    momentum: true,
    //您可以打开/关闭用户快速在屏幕上轻弹时执行的动量动画。关闭它可以大大提高性能。
    mouseWheelSpeed: 10,
    invertWheelDirection: false //激活鼠标滚轮支持时有意义，在这种情况下它只是反转滚动方向。（即向下滚动，反之亦然）。

  }, _zerodZtool.default.dataTypeTest(opt) === "object" ? opt : {});
  return new _iscroll.default(el, defaultopt);
}; //更新iscroll


exports.IScrollInstance = IScrollInstance;

var BuildScroll = function BuildScroll(el, opt) {
  var _this = this;

  this.nextScrollToTop = false;
  this.refresh = (0, _debounce.default)(function () {
    if (_this.nextScrollToTop) {
      _this.scroll.scrollTo(0, 0, 200);

      _this.nextScrollToTop = false;
    }

    _this.scroll.refresh();

    console.log("scrollRefresh");
  }, 60);
  this.callbackWare = [];

  if (isChrome() && !opt.useCustomScroll) {
    this.scroll = {
      x: 0,
      y: 0,
      on: function on(name, callback) {
        if (name === "scrollEnd") {
          _zerodZtool.default.on(el, "scroll", _this.scrollfunc);

          _this.callbackWare.push(callback);
        }
      },
      off: function off(name, callback) {
        if (name === "scrollEnd") {
          if (!_this.callbackWare.length) {
            _zerodZtool.default.off(el, "scroll", _this.scrollfunc);
          } else {
            var index = _this.callbackWare.indexOf(callback);

            if (index > -1) {
              _this.callbackWare.splice(index, 1);
            }
          }
        }
      },
      scrollTo: function scrollTo(x, y) {
        typeof x === "number" && (el.scrollLeft = Math.abs(x), _this.scroll.x = Math.abs(x));
        typeof y === "number" && (el.scrollTop = Math.abs(y), _this.scroll.y = Math.abs(y));
      },
      refresh: function refresh() {},
      _initEvents: function _initEvents() {},
      destroy: function destroy() {// zTool.off(el, "mouseenter", this.showScroll);
        // zTool.off(el, "mouseleave", this.hideScroll);
      }
    };

    this.scrollfunc = function (e) {
      _this.scroll.x = -el.scrollLeft;
      _this.scroll.y = -el.scrollTop;

      _this.callbackWare.forEach(function (ware) {
        typeof ware === "function" && ware(e);
      });
    }; // this.showScroll = () => {
    // 	el.style.overflow = "auto";
    // };
    // this.hideScroll = () => {
    // 	el.style.overflow = "hidden";
    // };
    // zTool.on(el, "mouseenter", this.showScroll);
    // zTool.on(el, "mouseleave", this.hideScroll);


    el.style.overflow = "auto";
  } else {
    this.scroll = IScrollInstance(el, opt); // console.log(this.scroll);
    //解决嵌套滚动条滚动问题

    var canscroll = false;
    var timer = null;

    var wheelhandle = function wheelhandle(e) {
      var scrollElement = e.target;

      while (scrollElement && scrollElement !== el && !["auto", "scroll"].includes(_zerodZtool.default.getStyle(scrollElement, "overflow-y"))) {
        scrollElement = scrollElement.parentElement;
      }

      if (scrollElement && el.contains(scrollElement)) {
        if (!scrollElement.bindingWheelScroll) {
          scrollDisableWheel(scrollElement, opt);
        }
      }

      if (_this.scroll.wrapperHeight - _this.scroll.y !== _this.scroll.scrollerHeight && _this.scroll.y !== 0) {
        e.stopPropagation();
        canscroll = true;
      } else {
        if (canscroll) {
          e.stopPropagation();
        }

        clearTimeout(timer);
        timer = setTimeout(function () {
          canscroll = false;
        }, 320);
      }
    };

    this.scroll.wrapper.addEventListener("wheel", wheelhandle, false);
    this.scroll.wrapper.addEventListener("mousewheel", wheelhandle, false);
    this.scroll.wrapper.addEventListener("DOMMouseScroll", wheelhandle, false);
    this.scroll.wrapper.addEventListener("mouseover", function () {
      canscroll = true;
    }, false);
  }
}; //


exports.BuildScroll = BuildScroll;

var scrollDisableWheel = function scrollDisableWheel(el) {
  var opt = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  if (isChrome() && !opt.useCustomScroll) return;
  el.bindingWheelScroll = true;
  var scrollEnd = true;

  var onWheel = function onWheel(e) {
    var delta = e.deltaY ? e.deltaY : -e.wheelDeltaY;

    if (delta > 0 && el.scrollTop + el.clientHeight < el.scrollHeight || delta < 0 && el.scrollTop > 0) {
      scrollEnd = false;
    }

    if (!scrollEnd) e.stopPropagation();
  };

  _zerodZtool.default.on(el, "wheel", onWheel);

  _zerodZtool.default.on(el, "mousewheel", onWheel);

  _zerodZtool.default.on(el, "DOMMouseScroll", onWheel);

  _zerodZtool.default.on(el, "scroll", function (e) {
    scrollEnd = el.scrollTop + el.clientHeight >= el.scrollHeight || el.scrollTop === 0;
  });
}; //监听盒子（div）尺寸变化


exports.scrollDisableWheel = scrollDisableWheel;

var listenDivSizeChange = function listenDivSizeChange(el, scrollFun) {
  var opt = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  if (isChrome() && !opt.useCustomScroll) return;
  new _ResizeSensor.default(el, (0, _debounce.default)(scrollFun, 60));
}; //生成 引导提示 introjs的实例，注意的是：此函数返回promise 需要在then((intro)>{})拿introjs的实例


exports.listenDivSizeChange = listenDivSizeChange;

var IntroInstance = function IntroInstance(opt) {
  //异步加载相关文件
  return _zerodZtool.default.loadFileList(["./static/introJs/introjs.min.css", "./static/introJs/themes/introjs-flattener.css", "./static/introJs/intro.min.js"]).then(function () {
    if (window.introJs) {
      var intro = window.introJs();
      intro.setOptions(Object.assign({
        nextLabel: "下一个",
        prevLabel: "上一个",
        doneLabel: "没有下一个，关闭",
        skipLabel: "关闭",
        disableInteraction: true,
        //是否让element可点击
        showProgress: true,
        //进度条
        exitOnOverlayClick: false,
        //是否点击遮罩层关闭
        showStepNumbers: true,
        //是否显示步骤数字
        showBullets: true,
        //是否显示步骤圆点
        // showButtons:true,
        exitOnEsc: false
      }, opt));
      return intro;
    }
  });
}; // 合并defaultConfig的属性生成新的config


exports.IntroInstance = IntroInstance;

var mergeConfig = function mergeConfig(defaultConfig, theConfig) {
  if (_zerodZtool.default.dataTypeTest(defaultConfig) === "object" && _zerodZtool.default.dataTypeTest(theConfig) === "object") {
    return (0, _merge.default)(_zerodZtool.default.deepCopy(defaultConfig), theConfig);
  }
};

exports.mergeConfig = mergeConfig;

var zTool = _objectSpread({}, _zerodZtool.default, {
  BuildScroll: BuildScroll,
  IScrollInstance: IScrollInstance,
  scrollDisableWheel: scrollDisableWheel,
  IntroInstance: IntroInstance,
  listenDivSizeChange: listenDivSizeChange,
  mergeConfig: mergeConfig,
  httpAjax: httpAjax
});

exports.zTool = zTool;
var _default = zTool;
exports.default = _default;

/***/ }),

/***/ "IvKo":
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

var _interopRequireDefault = __webpack_require__("TqRt");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _defineProperty2 = _interopRequireDefault(__webpack_require__("lSNA"));

__webpack_require__("dOvA");

var _axios = _interopRequireDefault(__webpack_require__("vDqi"));

var _v = _interopRequireDefault(__webpack_require__("xk4V"));

var _jsBase = __webpack_require__("J66h");

var _blueimpMd = _interopRequireDefault(__webpack_require__("jYEY"));

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(source, true).forEach(function (key) { (0, _defineProperty2.default)(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(source).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function formatNumber(str) {
  var t = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 2;
  str = str.toString();

  while (str.length < t) {
    str = "0" + str;
  }

  return str;
} //随机产生不重复id


function GenNonDuplicateID() {
  var randomLength = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 8;
  var id = Number(Math.random().toString().substr(3, randomLength) + Date.now()).toString();
  return id;
} //生成与后台协定的 X-Auth-Info


function getAuth() {
  var token = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "123";
  var key = GenNonDuplicateID().toString();
  var md5Key = (0, _blueimpMd.default)(key);
  var md5Token = (0, _blueimpMd.default)(token);
  var str = "";

  for (var index = 0; index < md5Key.length; index += 2) {
    var element = md5Key[index];
    str += element;
  }

  return "".concat(md5Token, ",").concat(key, ",").concat(_jsBase.Base64.encode("".concat(md5Token).concat(str)));
} //生成与后台协定的 X-Channel-Info


function getChannel(Auth) {
  var uuid = (0, _v.default)(); // console.log("uuid", uuid);

  var date = new Date();
  var year = date.getFullYear();
  var month = date.getMonth() + 1;
  var day = date.getDate();
  var hour = date.getHours();
  var minute = date.getMinutes();
  var second = date.getSeconds();
  var Msecond = date.getUTCMilliseconds();
  var str = "".concat(year).concat(formatNumber(month)).concat(formatNumber(day)).concat(formatNumber(hour)).concat(formatNumber(minute)).concat(formatNumber(second)).concat(formatNumber(Msecond, 3)); // console.log("timer", str);

  var tmp = "".concat(uuid, ",").concat(str, ",").concat((0, _blueimpMd.default)(Auth));

  var signature = _jsBase.Base64.encode(tmp);

  return "".concat(uuid, ",").concat(str, ",").concat(signature);
}

function getHeaders() {
  var userinfo = {};

  try {
    userinfo = JSON.parse(localStorage.getItem("main_save_userInfo"));
  } catch (e) {}

  if (userinfo && userinfo.token) {
    var Auth = getAuth(userinfo.token);
    return {
      "X-Token": userinfo.token,
      "X-Auth-Info": Auth,
      "X-Channel-Info": getChannel(Auth),
      "X-UserId": userinfo.userDO ? userinfo.userDO.id : ""
    };
  }

  return null;
} // import {const_notification} from '../constant';

/**
 *
 * @param {string} method //get || post || put    .....
 * @param {string} url
 * @param {object} query //接口参数，key，value 对应
 * @param {object} config //可配置请求头等，请看下面的config注释
 */


function httpAjax(method, url, query, config, noCallback) {
  //来自环境变量的basepath
  var basePath = process && Object({"NODE_ENV":"production"}) && Object({"NODE_ENV":"production"}).BASE_NAME ? Object({"NODE_ENV":"production"}).BASE_NAME.replace(/\/$/, "") : "";
  url = basePath + url; //请求错误弹出提示框方法

  var msgAlert = typeof window.globalMsgError === "function" ? window.globalMsgError : window.alert;
  config = config ? config : {};
  var headers = getHeaders();

  if (headers) {
    config = _objectSpread({}, config, {
      headers: _objectSpread({}, Object.prototype.toString.call(config.headers) == "[object Object]" ? config.headers : {}, {}, headers)
    });
  }

  method = method.toLowerCase(); //转小写

  switch (method) {
    case "get":
      //将get||delete请求传参处理成与post传参方式一致
      config = Object.assign(config, {
        params: query
      });
      break;

    case "delete":
      config = Object.assign(config, {
        data: query
      });
      break;

    case "location":
      window.location.assign(url);
      return;

    case "url":
      return url;

    case "url-promise":
      return Promise.resolve(url);

    case "post-params":
      method = "post";
      query = typeof query == "string" ? query : Object.keys(query).map(function (key) {
        return "".concat(key, "=").concat(query[key]);
      }).join("&");
      break;
  }

  var P = null;

  switch (method) {
    case "get":
      P = _axios.default[method](url, config);
      break;

    case "delete":
      P = _axios.default[method](url, config);
      break;

    default:
      P = _axios.default[method](url, query, config);
  }

  var promise = noCallback ? P : new Promise(function (resolve, reject) {
    P && P.then(function (result) {
      if (result.data && (result.data.code === -1 || result.data.code === 500)) {
        var msg = result.data.msg || result.data.data || "请求错误";
        msgAlert(msg);
        reject(result.data);
        return;
      }

      if (result.data && result.data.code == 403403) {
        msgAlert(result.data.msg ? result.data.msg : "用户未登录或身份已过期");
        reject(result.data);
        return;
      } // 后台请求返回的code=0是操作成功


      result.data && result.data.code === 0 ? resolve(result.data) : reject(result.data);
    }).catch(function (result) {
      var _newResult;

      var re = result.response && result.response.data ? result.response.data : {};
      var newResult = (_newResult = {
        msg: re.message
      }, (0, _defineProperty2.default)(_newResult, "msg", re.message), (0, _defineProperty2.default)(_newResult, "status", re.status), (0, _defineProperty2.default)(_newResult, "error", re.error), (0, _defineProperty2.default)(_newResult, "path", re.path), _newResult);

      if (newResult.msg || newResult.error) {
        msgAlert(newResult.msg || newResult.error);
      }

      reject(newResult);
    });
  });
  return promise;
}

var _default = httpAjax; // axios.interceptors.response.use(
// 	function(response) {
//         return Promise.reject(response.data);
// 		// return response.data.code === 0 ? response.data : Promise.reject(response.data);
// 	},
// 	function(error) {
//         console.log(error)
// 		// Do something with response error
// 		return error;
// 	},
// );

/* axios.request(config)
axios.get(url[, config])
axios.delete(url[, config])
axios.head(url[, config])
axios.post(url[, data[, config]])
axios.put(url[, data[, config]])
axios.patch(url[, data[, config]]) */
//config

/* {
    //请求的接口，在请求的时候，如axios.get(url,config);这里的url会覆盖掉config中的url
    url: '/user',

    // 请求方法，同上
    method: 'get', // default
    // 基础url前缀
    baseURL: 'https://some-domain.com/api/',


    transformRequest: [function (data) {
      // 这里可以在发送请求之前对请求数据做处理，比如form-data格式化等，这里可以使用开头引入的Qs（这个模块在安装axios的时候就已经安装了，不需要另外安装）
  　　data = Qs.stringify({});
      return data;
    }],

    transformResponse: [function (data) {
      // 这里提前处理返回的数据

      return data;
    }],

    // 请求头信息
    headers: {'X-Requested-With': 'XMLHttpRequest'},

    //parameter参数
    params: {
      ID: 12345
    },

    //post参数，使用axios.post(url,{},config);如果没有额外的也必须要用一个空对象，否则会报错
    data: {
      firstName: 'Fred'
    },

    //设置超时时间
    timeout: 1000,
    //返回数据类型
    responseType: 'json', // default


  } */

exports.default = _default;
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__("8oxB")))

/***/ }),

/***/ "aTWe":
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _interopRequireDefault = __webpack_require__("TqRt");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _extends2 = _interopRequireDefault(__webpack_require__("pVnL"));

var _classCallCheck2 = _interopRequireDefault(__webpack_require__("lwsE"));

var _createClass2 = _interopRequireDefault(__webpack_require__("W8MJ"));

var _possibleConstructorReturn2 = _interopRequireDefault(__webpack_require__("a1gu"));

var _getPrototypeOf2 = _interopRequireDefault(__webpack_require__("Nsbk"));

var _inherits2 = _interopRequireDefault(__webpack_require__("7W2i"));

var _react = _interopRequireDefault(__webpack_require__("q1tI"));

var _ZpureComponent2 = _interopRequireDefault(__webpack_require__("jY6J"));

var defaultValue = {};

var context = _react.default.createContext(defaultValue); //创建上下文


var Provider = context.Provider,
    Consumer = context.Consumer; //提供启用上下文的方法

var setConsumer = function setConsumer(ChildComponent) {
  return (
    /*#__PURE__*/
    function (_ZpureComponent) {
      (0, _inherits2.default)(ContextConsumer, _ZpureComponent);

      function ContextConsumer() {
        (0, _classCallCheck2.default)(this, ContextConsumer);
        return (0, _possibleConstructorReturn2.default)(this, (0, _getPrototypeOf2.default)(ContextConsumer).apply(this, arguments));
      }

      (0, _createClass2.default)(ContextConsumer, [{
        key: "render",
        value: function render() {
          var _this = this;

          return _react.default.createElement(Consumer, null, function (value) {
            return _react.default.createElement(ChildComponent, (0, _extends2.default)({}, _this.props, value, {
              ref: _this.props.forwardedRef
            }));
          });
        }
      }]);
      return ContextConsumer;
    }(_ZpureComponent2.default)
  );
};

var _default = {
  name: "ZerodRootContext",
  //上下文名称（必需）
  Provider: Provider,
  Consumer: Consumer,
  setConsumer: setConsumer
};
exports.default = _default;

/***/ }),

/***/ "cS7n":
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

var _interopRequireDefault = __webpack_require__("TqRt");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _configProvider = _interopRequireDefault(__webpack_require__("wEI+"));

var _classCallCheck2 = _interopRequireDefault(__webpack_require__("lwsE"));

var _createClass2 = _interopRequireDefault(__webpack_require__("W8MJ"));

var _possibleConstructorReturn2 = _interopRequireDefault(__webpack_require__("a1gu"));

var _getPrototypeOf3 = _interopRequireDefault(__webpack_require__("Nsbk"));

var _inherits2 = _interopRequireDefault(__webpack_require__("7W2i"));

var _react = _interopRequireDefault(__webpack_require__("q1tI"));

var _ZpureComponent2 = _interopRequireDefault(__webpack_require__("jY6J"));

var _reactRouterDom = __webpack_require__("eO8H");

var _zh_CN = _interopRequireDefault(__webpack_require__("xc/l"));

var _moment = _interopRequireDefault(__webpack_require__("wd/R"));

__webpack_require__("XDpg");

var _ZerodRootContext = _interopRequireDefault(__webpack_require__("aTWe"));

var _zTool = __webpack_require__("/sSO");

__webpack_require__("eoq3");

_moment.default.locale("zh-cn");

var fistLoad = true;

function hasPaceWatch() {
  if (window.Pace && fistLoad) {
    fistLoad = false;
    window.addEventListener("hashchange", function () {
      !window.isAppPortal && window.Pace && window.Pace.restart();
    }, false);
  }
}

function ZappHOC(pageConfig) {
  var defaultConfig = {
    rootRoutes: [],
    footerLinks: null,
    footerCopyright: null,
    routerType: "history",
    responseKeys: {
      listType: {
        list: "list",
        totalCount: "totalCount",
        totalPage: "totalPage"
      }
    }
  }; // console.log(dataType.isObject(pageConfig),pageConfig);

  defaultConfig = (0, _zTool.mergeConfig)(defaultConfig, pageConfig);

  if (defaultConfig.routerType == "hash") {
    hasPaceWatch();
  }

  console.log(Object({"NODE_ENV":"production"}));

  var App =
  /*#__PURE__*/
  function (_ZpureComponent) {
    (0, _inherits2.default)(App, _ZpureComponent);

    function App() {
      var _getPrototypeOf2;

      var _this;

      (0, _classCallCheck2.default)(this, App);

      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }

      _this = (0, _possibleConstructorReturn2.default)(this, (_getPrototypeOf2 = (0, _getPrototypeOf3.default)(App)).call.apply(_getPrototypeOf2, [this].concat(args)));
      _this.config = defaultConfig;
      _this.routes = _this.config.rootRoutes.map(function (item, i) {
        return item.redirect ? _react.default.createElement(_reactRouterDom.Route, {
          key: i,
          exact: true,
          path: item.path,
          render: function render() {
            return _react.default.createElement(_reactRouterDom.Redirect, {
              to: item.to
            });
          }
        }) : _react.default.createElement(_reactRouterDom.Route, {
          key: i,
          exact: item.exact,
          path: item.path,
          component: item.component
        });
      });
      return _this;
    }

    (0, _createClass2.default)(App, [{
      key: "render",
      value: function render() {
        var Router = this.config.routerType === "history" ? _reactRouterDom.BrowserRouter : _reactRouterDom.HashRouter;
        return _react.default.createElement(_configProvider.default, {
          locale: _zh_CN.default
        }, _react.default.createElement(_ZerodRootContext.default.Provider, {
          value: {
            footerLinks: this.config.footerLinks,
            footerCopyright: this.config.footerCopyright,
            responseKeys: this.config.responseKeys
          }
        }, _react.default.createElement(Router, {
          basename: process && Object({"NODE_ENV":"production"}) && Object({"NODE_ENV":"production"}).BASE_NAME ? Object({"NODE_ENV":"production"}).BASE_NAME : ""
        }, _react.default.createElement(_react.default.Fragment, null, this.routes))));
      }
    }]);
    return App;
  }(_ZpureComponent2.default);

  return App;
}

var _default = ZappHOC;
exports.default = _default;
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__("8oxB")))

/***/ }),

/***/ "dOvA":
/***/ (function(module, exports, __webpack_require__) {

"use strict";


// const promise = new Promise(function(resolve, reject) {
//     // ... some code
//     if (/* 异步操作成功 */){
//       resolve(value);
//     } else {
//       reject(error);
//     }
//   });
// Promise.prototype.then() 
// Promise.prototype.catch()
// Promise.race()
// Promise.resolve()
// Promise.reject()
// Promise除了以上原生自带的方法外，我们还可以扩展一些很有用的方法
// Promise 对象的回调链，不管以then方法或catch方法结尾，要是最后一个方法抛出错误，都有可能无法捕捉到（因为 Promise 内部的错误不会冒泡到全局）。因此，我们可以提供一个done方法，总是处于回调链的尾端，保证抛出任何可能出现的错误。
Promise.prototype.done = function (onFulfilled, onRejected) {
  this.then(onFulfilled, onRejected).catch(function (reason) {
    // 抛出一个全局错误
    setTimeout(function () {
      throw reason;
    }, 0);
  });
}; //   finally方法用于指定不管 Promise 对象最后状态如何，都会执行的操作。它与done方法的最大区别，它接受一个普通的回调函数作为参数，该函数不管怎样都必须执行。


Promise.prototype.finally = function (callback) {
  var P = this.constructor;
  return this.then(function (value) {
    return P.resolve(callback()).then(function () {
      return value;
    });
  }, function (reason) {
    return P.resolve(callback()).then(function () {
      throw reason;
    });
  });
};

/***/ }),

/***/ "eoq3":
/***/ (function(module, exports, __webpack_require__) {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ "jY6J":
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _interopRequireDefault = __webpack_require__("TqRt");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.ZpureComponent = void 0;

var _classCallCheck2 = _interopRequireDefault(__webpack_require__("lwsE"));

var _createClass2 = _interopRequireDefault(__webpack_require__("W8MJ"));

var _possibleConstructorReturn2 = _interopRequireDefault(__webpack_require__("a1gu"));

var _getPrototypeOf2 = _interopRequireDefault(__webpack_require__("Nsbk"));

var _inherits2 = _interopRequireDefault(__webpack_require__("7W2i"));

var _react = _interopRequireDefault(__webpack_require__("q1tI"));

var _zTool = __webpack_require__("/sSO");

function diff_Prev_Next(prev, next) {
  if (!_zTool.dataType.isObject(prev) || !_zTool.dataType.isObject(next)) return true;
  var propsKeys = Object.keys(next);
  var should = false;

  for (var index = 0; index < propsKeys.length; index++) {
    var key = propsKeys[index];

    if (prev[key] !== next[key] || _zTool.dataType.isFunction(next[key]) && !/^(on|export)[A-Z]{1}[\w\-]*$/.test(key)) {
      should = true;
      break;
    }
  }

  return should;
}

var ZpureComponent =
/*#__PURE__*/
function (_React$Component) {
  (0, _inherits2.default)(ZpureComponent, _React$Component);

  function ZpureComponent() {
    (0, _classCallCheck2.default)(this, ZpureComponent);
    return (0, _possibleConstructorReturn2.default)(this, (0, _getPrototypeOf2.default)(ZpureComponent).apply(this, arguments));
  }

  (0, _createClass2.default)(ZpureComponent, [{
    key: "shouldComponentUpdate",
    value: function shouldComponentUpdate(nextProps, nextState) {
      if (diff_Prev_Next(this.props, nextProps)) {
        return true;
      }

      if (diff_Prev_Next(this.state, nextState)) {
        return true;
      }

      return false;
    }
  }]);
  return ZpureComponent;
}(_react.default.Component);

exports.ZpureComponent = ZpureComponent;
var _default = ZpureComponent;
exports.default = _default;

/***/ }),

/***/ "mQv5":
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {

var _interopRequireDefault = __webpack_require__("TqRt");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hasClass = hasClass;
exports.addClass = addClass;
exports.removeClass = removeClass;
exports.setStyle = setStyle;
exports.filterQuery = filterQuery;
exports.digitUppercase = digitUppercase;
exports.isUrl = isUrl;
exports.isHttpStart = isHttpStart;
exports.formatterMapKey = formatterMapKey;
exports.itemsFromTree = itemsFromTree;
exports.removeItemFromTree = removeItemFromTree;
exports.replaceItemFromTree = replaceItemFromTree;
exports.pushItemToTree = pushItemToTree;
exports.unshiftItemToTree = unshiftItemToTree;
exports.insertBeforeItemFromTree = insertBeforeItemFromTree;
exports.insertAfterItemFromTree = insertAfterItemFromTree;
exports.isWhiteColor = isWhiteColor;
exports.turnMapKeys = turnMapKeys;
exports.turnLabelOrValue = turnLabelOrValue;
exports.default = exports.zTool = exports.checkDevices = exports.parseQueryString = exports.EetoString = exports.GenNonDuplicateID = exports.formatDate = exports.arrayFilterBy = exports.deepCopy = exports.deepCopyArray = exports.deepCopyObject = exports.dataTypeTest = exports.dataType = exports.firstWordToUpperCase = exports.getStyle = exports.once = exports.off = exports.on = exports.onNoRepeatEvent = exports.loadFileList = void 0;

var _defineProperty2 = _interopRequireDefault(__webpack_require__("lSNA"));

var _typeof2 = _interopRequireDefault(__webpack_require__("cDf5"));

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(source, true).forEach(function (key) { (0, _defineProperty2.default)(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(source).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

/*
 * @Author: zgt
 * @Date: 2019-06-24 11:04:00
 * @LastEditors: zgt
 * @LastEditTime: 2019-09-07 15:48:07
 * @Description: file content
 */

/*加载一批文件，_files:文件路径数组,可包括js,css,less文件,isSequence 是否按数组的顺序加载*/
var loadFileList = function () {
  /* 已加载文件缓存列表,用于判断文件是否已加载过，若已加载则不再次加载*/
  var classcodes = [];
  /*加载JS文件,url:文件路径*/

  var loadFile = function loadFile(url) {
    var newUrl = "";
    var protos = null;

    if (dataTypeTest(url) === "object") {
      newUrl = url.url;
      protos = url.protos;
    } else {
      newUrl = url;
    }

    if (!FileIsExt(classcodes, newUrl)) {
      var ThisType = GetFileType(newUrl);
      var fileObj = null;

      if (ThisType == ".js" || ThisType == "http") {
        fileObj = document && document.createElement("script");
        fileObj.src = newUrl;
      } else if (ThisType == ".css") {
        fileObj = document && document.createElement("link");
        fileObj.href = newUrl;
        fileObj.type = "text/css";
        fileObj.rel = "stylesheet";
      } else if (ThisType == ".less") {
        fileObj = document && document.createElement("link");
        fileObj.href = newUrl;
        fileObj.type = "text/css";
        fileObj.rel = "stylesheet/less";
      }

      if (dataTypeTest(protos) === "object" && fileObj) {
        Object.keys(protos).forEach(function (key) {
          fileObj[key] = protos[key];
        });
      }

      if (fileObj) return new Promise(function (resolve, reject) {
        fileObj.onload = fileObj.onreadystatechange = function () {
          if (!this.readyState || "loaded" === this.readyState || "complete" === this.readyState) {
            // success();
            classcodes.push(newUrl);
            resolve();
          }
        };

        document && document.getElementsByTagName("head")[0].appendChild(fileObj);
      });else return Promise.resolve();
    } else {
      return Promise.resolve();
    }
  };
  /*获取文件类型,后缀名，小写*/


  var GetFileType = function GetFileType(url) {
    if (url != null && url.length > 0) {
      var lasindex = url.lastIndexOf(".");

      if (lasindex > -1) {
        return url.substr(url.lastIndexOf(".")).toLowerCase();
      } else if (/^(http:|https:)/.test(url)) {
        return "http";
      }
    }

    return "";
  };
  /*文件是否已加载*/


  var FileIsExt = function FileIsExt(FileArray, _url) {
    if (FileArray != null && FileArray.length > 0) {
      var len = FileArray.length;

      for (var i = 0; i < len; i++) {
        if (FileArray[i] == _url) {
          return true;
        }
      }
    }

    return false;
  };

  return function (_files, isSequence) {
    var FileArray = [];

    if (dataTypeTest(_files) === "array") {
      FileArray = _files;
    } else if (dataTypeTest(_files) === "string") {
      /*如果文件列表是字符串，则用,切分成数组*/
      FileArray = _files.split(",");
    }

    if (FileArray != null && FileArray.length > 0) {
      var LoadedCount = 0;

      if (isSequence) {
        //依次加载
        return new Promise(function (resolve, reject) {
          var len = FileArray.length;
          var P = loadFile(FileArray.shift());

          var _loop = function _loop() {
            var url = FileArray[i];
            P = P.then(function () {
              LoadedCount++;
              return loadFile(url);
            });
          };

          for (var i = 0; i < len - 1; i++) {
            _loop();
          }

          P.then(function () {
            LoadedCount++;

            if (LoadedCount === len) {
              resolve();
            }
          });
        });
      } else {
        //非依次加载
        return new Promise(function (resolve, reject) {
          var len = FileArray.length;

          for (var i = 0; i < len; i++) {
            var url = FileArray[i];
            loadFile(url).then(function () {
              LoadedCount++;

              if (LoadedCount === len) {
                resolve();
              }
            });
          }
        });
      }
    } else {
      return Promise.reject();
    }
  };
}(); // import Vue from 'vue';
// const isServer = Vue.prototype.$isServer;


exports.loadFileList = loadFileList;
var isServer = false;
var SPECIAL_CHARS_REGEXP = /([\:\-\_]+(.))/g;
var MOZ_HACK_REGEXP = /^moz([A-Z])/;
var ieVersion = isServer ? 0 : Number(document ? document.documentMode : 0);
/* 去掉字符串首尾空格 */

var trim = function trim(string) {
  return (string || "").replace(/^[\s\uFEFF]+|[\s\uFEFF]+$/g, "");
};
/* istanbul ignore next */


var camelCase = function camelCase(name) {
  return name.replace(SPECIAL_CHARS_REGEXP, function (_, separator, letter, offset) {
    return offset ? letter.toUpperCase() : letter;
  }).replace(MOZ_HACK_REGEXP, "Moz$1");
}; //绑定不重复的事件


var hasListenerEvents = [];

var onNoRepeatEvent = function onNoRepeatEvent(element, event, handler) {
  var hasBanded = null;
  var index = 0;

  for (var i = 0; i < hasListenerEvents.length; i++) {
    var item = hasListenerEvents[i];

    if (item.element == element && item.event == event) {
      hasBanded = item;
      index = i;
      break;
    }
  }

  if (hasBanded) {
    off(hasBanded.element, hasBanded.event, hasBanded.handler);
    on(element, event, handler);
    hasListenerEvents.splice(index, 1, {
      element: element,
      event: event,
      handler: handler
    });
  } else {
    on(element, event, handler);
    hasListenerEvents.push({
      element: element,
      event: event,
      handler: handler
    });
  }
};
/* 绑定事件on(element, event, handler) */


exports.onNoRepeatEvent = onNoRepeatEvent;

var on = function () {
  if (!isServer && document && document.addEventListener) {
    return function (element, event, handler) {
      if (element && event && handler) {
        element.addEventListener(event, handler, false);
      }
    };
  } else {
    return function (element, event, handler) {
      if (element && event && handler) {
        element.attachEvent("on" + event, handler);
      }
    };
  }
}();
/* 绑定事件off(element, event, handler) */


exports.on = on;

var off = function () {
  if (!isServer && document && document.removeEventListener) {
    return function (element, event, handler) {
      if (element && event) {
        element.removeEventListener(event, handler, false);
      }
    };
  } else {
    return function (element, event, handler) {
      if (element && event) {
        element.detachEvent("on" + event, handler);
      }
    };
  }
}();
/* 绑定一次性事件one(element, event, handler) */


exports.off = off;

var once = function once(el, event, fn) {
  var listener = function listener() {
    if (fn) {
      fn.apply(this, arguments);
    }

    off(el, event, listener);
  };

  on(el, event, listener);
};
/* 是否存在某个样式类名 */


exports.once = once;

function hasClass(el, cls) {
  if (!el || !cls) return false;
  if (cls.indexOf(" ") !== -1) throw new Error("className should not contain space.");

  if (el.classList) {
    return el.classList.contains(cls);
  } else {
    return (" " + el.className + " ").indexOf(" " + cls + " ") > -1;
  }
}
/* 添加className */


function addClass(el, cls) {
  if (!el) return;
  var curClass = el.className;
  var classes = (cls || "").split(" ");

  for (var i = 0, j = classes.length; i < j; i++) {
    var clsName = classes[i];
    if (!clsName) continue;

    if (el.classList) {
      el.classList.add(clsName);
    } else {
      if (!hasClass(el, clsName)) {
        curClass += " " + clsName;
      }
    }
  }

  if (!el.classList) {
    el.className = curClass;
  }
}
/* 移除className */


function removeClass(el, cls) {
  if (!el || !cls) return;
  var classes = cls.split(" ");
  var curClass = " " + el.className + " ";

  for (var i = 0, j = classes.length; i < j; i++) {
    var clsName = classes[i];
    if (!clsName) continue;

    if (el.classList) {
      el.classList.remove(clsName);
    } else {
      if (hasClass(el, clsName)) {
        curClass = curClass.replace(" " + clsName + " ", " ");
      }
    }
  }

  if (!el.classList) {
    el.className = trim(curClass);
  }
}
/* 获取某个元素的某个样式 */


var getStyle = ieVersion < 9 ? function (element, styleName) {
  if (isServer) return;
  if (!element || !styleName) return null;
  styleName = camelCase(styleName);

  if (styleName === "float") {
    styleName = "styleFloat";
  }

  try {
    switch (styleName) {
      case "opacity":
        try {
          return element.filters.item("alpha").opacity / 100;
        } catch (e) {
          return 1.0;
        }

      default:
        return element.style[styleName] || element.currentStyle ? element.currentStyle[styleName] : null;
    }
  } catch (e) {
    return element.style[styleName];
  }
} : function (element, styleName) {
  if (isServer) return;
  if (!element || !styleName) return null;
  styleName = camelCase(styleName);

  if (styleName === "float") {
    styleName = "cssFloat";
  }

  try {
    var computed = document && document.defaultView.getComputedStyle(element, "");
    return element.style[styleName] || computed ? computed[styleName] : null;
  } catch (e) {
    return element.style[styleName];
  }
};
/* 给某个元素设置 style */

exports.getStyle = getStyle;

function setStyle(element, styleName, value) {
  if (!element || !styleName) return;

  if ((0, _typeof2.default)(styleName) === "object") {
    for (var prop in styleName) {
      if (styleName.hasOwnProperty(prop)) {
        setStyle(element, prop, styleName[prop]);
      }
    }
  } else {
    styleName = camelCase(styleName);

    if (styleName === "opacity" && ieVersion < 9) {
      element.style.filter = isNaN(value) ? "" : "alpha(opacity=" + value * 100 + ")";
    } else {
      element.style[styleName] = value;
    }
  }
} //首字母大写


var firstWordToUpperCase = function firstWordToUpperCase(str) {
  return str.toLowerCase().replace(/( |^)[a-z]/g, function (L) {
    return L.toUpperCase();
  });
};

exports.firstWordToUpperCase = firstWordToUpperCase;
var data_types = {
  array: "[object Array]",
  object: "[object Object]",
  string: "[object String]",
  number: "[object Number]",
  boolean: "[object Boolean]",
  null: "[object Null]",
  undefined: "[object Undefined]",
  function: "[object Function]",
  date: "[object Date]",
  symbol: "[object Symbol]",
  set: "[object Set]",
  Map: "[object Map]",
  regExp: "[object RegExp]",
  json: "[object JSON]",
  //JSON对象
  promise: "[object Promise]",
  //Promise对象
  element: /HTML\w+Element/g
}; // 包含isArray,isObject,isString,isPromise等等

var dataType = {};
exports.dataType = dataType;
Object.keys(data_types).forEach(function (key) {
  dataType["is" + firstWordToUpperCase(key)] = function (item) {
    return dataTypeTest(item) === key;
  };
}); //检测数据类型 ，返回对应数据类型的名称

var dataTypeTest = function dataTypeTest(item) {
  var types = Object.keys(data_types);
  var hasType = null;
  var value = Object.prototype.toString.call(item);

  for (var index = 0; index < types.length; index++) {
    var key = types[index];

    if (value === data_types[key] || Object.prototype.toString.call(data_types[key]) === "[object RegExp]" && value.search(data_types[key]) > -1) {
      hasType = key;
      break;
    }
  }

  return hasType;
}; //深度复制对象


exports.dataTypeTest = dataTypeTest;

var deepCopyObject = function deepCopyObject(obj) {
  if (!obj || dataTypeTest(obj) !== "object") return {}; // return JSON.parse(JSON.stringify(obj));

  var newobj = {};

  for (var key in obj) {
    var type = dataTypeTest(obj[key]);

    switch (type) {
      case "string":
      case "null":
      case "number":
      case "undefined":
      case "function":
      case "boolean":
      case "symbol":
      case "set":
      case "map":
      case "json":
      case "promise":
      case "regExp":
      case "element":
        newobj[key] = obj[key];
        break;

      case "date":
        newobj[key] = obj[key].getTime();
        break;

      case "array":
        newobj[key] = deepCopyArray(obj[key]);
        break;

      case "object":
        newobj[key] = deepCopyObject(obj[key]);
        break;
    }
  }

  return newobj;
}; //深度复制数组


exports.deepCopyObject = deepCopyObject;

var deepCopyArray = function deepCopyArray(array) {
  if (!array || dataTypeTest(array) !== "array") return []; // return JSON.parse(JSON.stringify(array));

  var newarray = [];

  for (var i = 0; i < array.length; i++) {
    var item = array[i];
    var type = dataTypeTest(item);

    switch (type) {
      case "string":
      case "null":
      case "number":
      case "undefined":
      case "function":
      case "boolean":
      case "symbol":
      case "set":
      case "map":
      case "json":
      case "promise":
      case "regExp":
      case "element":
        newarray.push(item);
        break;

      case "date":
        newarray.push(item.getTime());
        break;

      case "array":
        newarray.push(deepCopyArray(item));
        break;

      case "object":
        newarray.push(deepCopyObject(item));
        break;
    }
  }

  return newarray;
}; //深度复制数组/对象


exports.deepCopyArray = deepCopyArray;

var deepCopy = function deepCopy(value) {
  switch (dataTypeTest(value)) {
    case "object":
      return deepCopyObject(value);

    case "array":
      return deepCopyArray(value);

    default:
      return value;
  }
}; //根据条件过滤数组，只能过滤两种情况：一、数组里面全是对象，二、数组里面全是字符串或者其他类型的


exports.deepCopy = deepCopy;

var arrayFilterFn = function arrayFilterFn(array, property) {
  var type = dataTypeTest(property);
  return array.filter(function (item) {
    var itemtype = dataTypeTest(item);

    if (itemtype === "object" && type === "object") {
      var hasItem = false;
      Object.keys(property).forEach(function (el) {
        hasItem = item[el] === property[el];
      });
      return hasItem;
    } else {
      return item === property;
    }
  });
}; //用法一如：var arr=[{name:"水果",value:1},{name:"蔬菜",value:2}] ,arrayFilterBy(arr,{value:2})得到[{name:"蔬菜",value:2}]
//用法二如：var arr=[1,2,3,2,4,3,5,3]   arrayFilterBy(arr,3)得到 [3,3,3]
//用法三如：var arr=[{name:"水果",value:1},{name:"蔬菜",value:2},{name:"草莓",value:4}] ,arrayFilterBy(arr,[{value:2},{value:4}])得到[{name:"蔬菜",value:2},{name:"草莓",value:4}]
//用法四如：var arr=[1,2,3,2,4,3,5,3]   arrayFilterBy(arr,[3,2])得到 [3,3,3,2,2]


var arrayFilterBy = function arrayFilterBy(array, property) {
  //property可以是{name:'zgt'}或者'zgt'
  if (!array || dataTypeTest(array) !== "array") return [];
  var type = dataTypeTest(property);

  if (type === "array") {
    var newarray = [];
    property.forEach(function (item) {
      newarray = newarray.concat(arrayFilterFn(array, item));
    });
    return newarray;
  } else {
    return arrayFilterFn(array, property);
  }
}; //格式时间 支持时间戳转yyyy-MM-dd HH:mm:ss格式
//支持时间戳转 yyyy-mm-dd格式 ，支持yyyy/mm/dd转yyyy-mm-dd，不支持yyyy-mm-dd转yyyy/mm/dd


exports.arrayFilterBy = arrayFilterBy;

var formatDate = function formatDate(time, format) {
  if (!time) return "";
  var date = dataTypeTest(time) === "date" ? time : new Date(time);
  var ft = trim(format || "yyyy-MM-dd HH:mm:ss").split(" ");
  var newdate = "";
  var symbol = "-";
  if (!ft[0]) return;

  if (/[-]+/.test(ft[0]) && !/[/]+/.test(ft[0])) {
    symbol = "-";
  } else if (!/[-]+/.test(ft[0]) && /[/]+/.test(ft[0])) {
    symbol = "/";
  } else if (/[:]+/.test(ft[0])) {
    symbol = ":";
  }

  var addZero = function addZero(value) {
    if (Number(value) < 10) {
      return "0" + value;
    } else {
      return value;
    }
  };

  var setTimes = function setTimes(fts, symbol) {
    var newTime = "";
    var timeFtsLen = fts.split(":").length;

    switch (timeFtsLen) {
      case 1:
        newTime += addZero(date.getHours());
        break;

      case 2:
        newTime += addZero(date.getHours()) + symbol + addZero(date.getMinutes());
        break;

      default:
        newTime += addZero(date.getHours()) + symbol + addZero(date.getMinutes()) + symbol + addZero(date.getSeconds());
        break;
    }

    return newTime;
  };

  if (symbol === ":") {
    newdate += setTimes(ft[0], symbol);
  } else {
    var timeFtsLen = ft[0].split(symbol).length;

    switch (timeFtsLen) {
      case 1:
        newdate += date.getFullYear();
        break;

      case 2:
        newdate += date.getFullYear() + symbol + addZero(date.getMonth() + 1);
        break;

      default:
        newdate += date.getFullYear() + symbol + addZero(date.getMonth() + 1) + symbol + addZero(date.getDate());
        break;
    }

    if (ft[1]) {
      symbol = ":";
      newdate += " ";
      newdate += setTimes(ft[1], symbol);
    }
  }

  return newdate;
}; //随机产生不重复id


exports.formatDate = formatDate;

var GenNonDuplicateID = function GenNonDuplicateID() {
  var randomLength = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 8;
  var id = Number(Math.random().toString().substr(3, randomLength) + Date.now()).toString();
  return id;
}; //科学计数法转 字符串
//如 6.5E8 得 "650000000"
//如 6.5e-7得 "0.65000000"


exports.GenNonDuplicateID = GenNonDuplicateID;

var EetoString = function EetoString(num) {
  var str = num.toString();
  var reg = /^((\d+.\d+)|(\d+))([Ee])([\-]?\d+)$/;
  var arr,
      len,
      zerod = "";
  /*6e7或6e+7 都会自动转换数值*/

  if (!reg.test(str)) {
    return num;
  } else {
    /*6e-7 需要手动转换*/
    arr = reg.exec(str);
    len = Math.abs(arr[5]) - 1;

    for (var i = 0; i < len; i++) {
      zerod += "0";
    }

    return "0." + zerod + (arr[2] ? arr[1] : arr[3]).replace(".", "");
  }
}; // 过滤一个对象中有用的的属性，permissiveKys是允许的属性名数组


exports.EetoString = EetoString;

function filterQuery(permissiveKys, query) {
  var newQuery = {};
  Object.keys(query).forEach(function (key) {
    if (permissiveKys.includes(key)) {
      newQuery[key] = query[key];
    }
  });
  return newQuery;
} //将url问号参数转成map对象


var parseQueryString = function parseQueryString(url) {
  if (isUrl(url)) {
    var searchurl = url.split("?");
    url = searchurl.length > 1 ? "?" + searchurl[1] : searchurl[0];
  }

  var reg_url = /^\?([\w\W]+)$/;
  var reg_para = /([^&=]+)=([\w\W]*?)(&|$|#)/g,
      arr_url = reg_url.exec(url),
      ret = {};

  if (arr_url && arr_url[1]) {
    var str_para = arr_url[1],
        result;

    while ((result = reg_para.exec(str_para)) != null) {
      ret[result[1]] = result[2];
    }
  }

  return ret;
}; //检测移动端设备


exports.parseQueryString = parseQueryString;

var checkDevices = function checkDevices() {
  var userAgent = global.navigator.userAgent;

  if (/Android/i.test(userAgent)) {
    return "android";
  } else if (/iPhone|iPad|iPod/i.test(userAgent)) {
    return "ios";
  } else if (/WindowsWechat/i.test(userAgent)) {
    return "WindowsWechat";
  } else if (/MicroMessenger/i.test(userAgent)) {
    return "MicroMessenger";
  } // const inAndroid = /Android/i.test(userAgent)
  // const inIOS = /iPhone|iPad|iPod/i.test(userAgent)
  // const inMicroMessenger = /MicroMessenger/i.test(userAgent)
  // const inWindowsWechat = /WindowsWechat/i.test(userAgent)

}; //新增
// export function fixedZero(val) {
// 	return val * 1 < 10 ? `0${val}` : val;
// }
// export function getTimeDistance(type) {
// 	const now = new Date();
// 	const oneDay = 1000 * 60 * 60 * 24;
// 	if (type === "today") {
// 		now.setHours(0);
// 		now.setMinutes(0);
// 		now.setSeconds(0);
// 		return [moment(now), moment(now.getTime() + (oneDay - 1000))];
// 	}
// 	if (type === "week") {
// 		let day = now.getDay();
// 		now.setHours(0);
// 		now.setMinutes(0);
// 		now.setSeconds(0);
// 		if (day === 0) {
// 			day = 6;
// 		} else {
// 			day -= 1;
// 		}
// 		const beginTime = now.getTime() - day * oneDay;
// 		return [moment(beginTime), moment(beginTime + (7 * oneDay - 1000))];
// 	}
// 	if (type === "month") {
// 		const year = now.getFullYear();
// 		const month = now.getMonth();
// 		const nextDate = moment(now).add(1, "months");
// 		const nextYear = nextDate.year();
// 		const nextMonth = nextDate.month();
// 		return [
// 			moment(`${year}-${fixedZero(month + 1)}-01 00:00:00`),
// 			moment(moment(`${nextYear}-${fixedZero(nextMonth + 1)}-01 00:00:00`).valueOf() - 1000),
// 		];
// 	}
// 	if (type === "year") {
// 		const year = now.getFullYear();
// 		return [moment(`${year}-01-01 00:00:00`), moment(`${year}-12-31 23:59:59`)];
// 	}
// }
// export function getPlainNode(nodeList, parentPath = "") {
// 	const arr = [];
// 	nodeList.forEach((node) => {
// 		const item = node;
// 		item.path = `${parentPath}/${item.path || ""}`.replace(/\/+/g, "/");
// 		item.exact = true;
// 		if (item.children && !item.component) {
// 			arr.push(...getPlainNode(item.children, item.path));
// 		} else {
// 			if (item.children && item.component) {
// 				item.exact = false;
// 			}
// 			arr.push(item);
// 		}
// 	});
// 	return arr;
// }


exports.checkDevices = checkDevices;

function accMul(arg1, arg2) {
  var m = 0;
  var s1 = arg1.toString();
  var s2 = arg2.toString();
  m += s1.split(".").length > 1 ? s1.split(".")[1].length : 0;
  m += s2.split(".").length > 1 ? s2.split(".")[1].length : 0;
  return Number(s1.replace(".", "")) * Number(s2.replace(".", "")) / Math.pow(10, m);
}

function digitUppercase(n) {
  var fraction = ["角", "分"];
  var digit = ["零", "壹", "贰", "叁", "肆", "伍", "陆", "柒", "捌", "玖"];
  var unit = [["元", "万", "亿"], ["", "拾", "佰", "仟", "万"]];
  var num = Math.abs(n);
  var s = "";
  fraction.forEach(function (item, index) {
    s += (digit[Math.floor(accMul(num, 10 * Math.pow(10, index))) % 10] + item).replace(/零./, "");
  });
  s = s || "整";
  num = Math.floor(num);

  for (var i = 0; i < unit[0].length && num > 0; i += 1) {
    var p = "";

    for (var j = 0; j < unit[1].length && num > 0; j += 1) {
      p = digit[num % 10] + unit[1][j] + p;
      num = Math.floor(num / 10);
    }

    s = p.replace(/(零.)*零$/, "").replace(/^$/, "零") + unit[0][i] + s;
  }

  return s.replace(/(零.)*零元/, "元").replace(/(零.)+/g, "零").replace(/^整$/, "零元整");
}
/* eslint no-useless-escape:0 */


var reg = /(((^https?:(?:\/\/)?)(?:[-;:&=\+\$,\w]+@)?[A-Za-z0-9.-]+(?::\d+)?|(?:www.|[-;:&=\+\$,\w]+@)[A-Za-z0-9.-]+)((?:\/[\+~%\/.\w-_]*)?\??(?:[-\+=&;%@.\w_]*)#?(?:[\w\?\=\&]*))?)$/;

function isUrl(path) {
  return reg.test(path);
}

var startHttpReg = /^(http:|https:)[\w\?\&\.\#\%\*\@\$\!\~\/]*/;

function isHttpStart(path) {
  return startHttpReg.test(path);
} // 将从后台获取的导航数据转成route的需要的key


function formatterMapKey(data) {
  var mapKey = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var parentPath = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : "/";
  var notParPath = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  // const notParPath = typeof parentPath == "boolean" && !parentPath;
  mapKey = Object.assign({
    iconClass: "iconClass",
    path: "path",
    name: "name",
    children: "children"
  }, mapKey ? mapKey : {});
  return data.map(function (item) {
    var path = item[mapKey.path];
    if (!notParPath && !isHttpStart(path)) path = parentPath + path.replace(/^\/*/, "");

    var newData = _objectSpread({}, item, {
      iconClass: item[mapKey.iconClass] !== undefined ? item[mapKey.iconClass] : "smile-o",
      path: path,
      parPath: parentPath.replace(/\/$/, ""),
      name: item[mapKey.name]
    });

    if (Array.isArray(item[mapKey.children]) && item[mapKey.children].length) {
      newData[mapKey.children] = formatterMapKey(item[mapKey.children], mapKey, "".concat(path, "/"), notParPath);
    }

    return newData;
  });
}

function itemsFromTree(_ref) {
  var tree = _ref.tree,
      sourceItem = _ref.sourceItem,
      item = _ref.item,
      keyObj = _ref.keyObj,
      action = _ref.action,
      parentItem = _ref.parentItem,
      condition = _ref.condition;
  var finished = false;
  keyObj = dataType.isObject(keyObj) ? keyObj : {
    id: "id",
    children: "children"
  };

  for (var index = 0; index < tree.length; index++) {
    var currentItem = tree[index];

    if (dataType.isFunction(condition) ? condition(currentItem[keyObj.id], sourceItem[keyObj.id]) : currentItem[keyObj.id] === sourceItem[keyObj.id]) {
      dataType.isFunction(action) && action({
        tree: tree,
        currentItem: currentItem,
        item: item,
        index: index,
        keyObj: keyObj,
        parentItem: parentItem
      });
      finished = true;
    } else if (Array.isArray(currentItem[keyObj.children])) {
      finished = itemsFromTree({
        tree: currentItem[keyObj.children],
        sourceItem: sourceItem,
        item: item,
        keyObj: keyObj,
        action: action,
        parentItem: currentItem
      });
    }

    if (finished) break;
  }

  return finished;
}
/**
 *用于移除json中一项数据
 *
 * @export
 * @param {object} obj { tree:array, sourceItem:object, keyObj:{id:"id",children:"children"} }
 * @returns
 */


function removeItemFromTree(obj) {
  // { tree, sourceItem, keyObj }
  var newobj = deepCopy(obj);

  newobj.action = function (_ref2) {
    var tree = _ref2.tree,
        currentItem = _ref2.currentItem,
        index = _ref2.index,
        keyObj = _ref2.keyObj;
    tree.splice(index, 1);
  };

  if (itemsFromTree(newobj)) {
    return newobj.tree;
  }
}
/**
 *用item数据替换json中一项sourceItem数据
 *
 * @export
 * @param {object} obj { tree:array, sourceItem:object,item:object, keyObj:{id:"id",children:"children"} }
 * @returns
 */


function replaceItemFromTree(obj) {
  var newobj = deepCopy(obj);

  newobj.action = function (_ref3) {
    var tree = _ref3.tree,
        currentItem = _ref3.currentItem,
        item = _ref3.item,
        index = _ref3.index,
        keyObj = _ref3.keyObj;
    tree.splice(index, 1, item);
  };

  if (itemsFromTree(newobj)) {
    return newobj.tree;
  }
}
/**
 *用于json中某项sourceItem数据的children末端添加一项item数据
 *
 * @export
 * @param {object} obj { tree:array, sourceItem:object,item:object, keyObj:{id:"id",children:"children"} }
 * @returns
 */


function pushItemToTree(obj) {
  var newobj = deepCopy(obj);

  newobj.action = function (_ref4) {
    var tree = _ref4.tree,
        currentItem = _ref4.currentItem,
        item = _ref4.item,
        index = _ref4.index,
        keyObj = _ref4.keyObj;

    if (!Array.isArray(currentItem[keyObj.children])) {
      currentItem[keyObj.children] = [];
    }

    currentItem[keyObj.children].push(item);
  };

  if (itemsFromTree(newobj)) {
    return newobj.tree;
  }
}
/**
 *用于json中某项sourceItem数据的children头端添加一项item数据
 *
 * @export
 * @param {object} obj { tree:array, sourceItem:object,item:object, keyObj:{id:"id",children:"children"} }
 * @returns
 */


function unshiftItemToTree(obj) {
  var newobj = deepCopy(obj);

  newobj.action = function (_ref5) {
    var tree = _ref5.tree,
        currentItem = _ref5.currentItem,
        item = _ref5.item,
        index = _ref5.index,
        keyObj = _ref5.keyObj;

    if (!Array.isArray(currentItem[keyObj.children])) {
      currentItem[keyObj.children] = [];
    }

    currentItem[keyObj.children].unshift(item);
  };

  if (itemsFromTree(newobj)) {
    return newobj.tree;
  }
}
/**
 *用于将一项item数据插入在json中某项sourceItem数据之前
 *
 * @export
 * @param {object} obj { tree:array, sourceItem:object,item:object, keyObj:{id:"id",children:"children"} }
 * @returns
 */


function insertBeforeItemFromTree(obj) {
  var newobj = deepCopy(obj);

  newobj.action = function (_ref6) {
    var tree = _ref6.tree,
        currentItem = _ref6.currentItem,
        item = _ref6.item,
        index = _ref6.index,
        keyObj = _ref6.keyObj;
    tree.splice(index, 0, item);
  };

  if (itemsFromTree(newobj)) {
    return newobj.tree;
  }
}
/**
 *用于将一项item数据插入在json中某项sourceItem数据之后
 *
 * @export
 * @param {object} obj { tree:array, sourceItem:object,item:object, keyObj:{id:"id",children:"children"} }
 * @returns
 */


function insertAfterItemFromTree(obj) {
  var newobj = deepCopy(obj);

  newobj.action = function (_ref7) {
    var tree = _ref7.tree,
        currentItem = _ref7.currentItem,
        item = _ref7.item,
        index = _ref7.index,
        keyObj = _ref7.keyObj;
    tree.splice(index + 1, 0, item);
  };

  if (itemsFromTree(newobj)) {
    return newobj.tree;
  }
} //判断是白色颜色值


function isWhiteColor(str) {
  return typeof str === "string" && (str.search(/^\#(f{3}$|f{6}$)/i) > -1 || str.search(/^rgb(a)?\(255,255,255/) > -1);
}
/**
 * @description: 转换tree数据的键名
 * @param tree {array}
 * @param srcMapKeys {object} 默认 { label: "label", value: "value", children: "children" },
 * @param distMapKeys {object} 默认 { label: "label", value: "value", children: "children" }
 * @param extands {object} 默认 {}  可以在tree中加入定义的字段
 * @param valueToString {boolean} 默认 false 是否把 value (有可能是数字) 转成 string 类型的
 * @param includesSourceItem {boolean} 默认 true  除了label，value字段，是否保留tree中的其他字段
 * @return: newTree
 */


function turnMapKeys(tree) {
  var srcMapKeys = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
    label: "label",
    value: "value",
    children: "children"
  };
  var distMapKeys = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {
    label: "label",
    value: "value",
    children: "children"
  };
  var extands = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
  var valueToString = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : false;
  var includesSourceItem = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : true;
  return Array.isArray(tree) ? tree.map(function (item) {
    var _objectSpread2;

    var children = item[srcMapKeys.children];
    var sourceItem = includesSourceItem ? item : {};
    delete sourceItem[srcMapKeys.children];

    var newItem = _objectSpread({}, sourceItem, (_objectSpread2 = {}, (0, _defineProperty2.default)(_objectSpread2, distMapKeys.label, item[srcMapKeys.label]), (0, _defineProperty2.default)(_objectSpread2, distMapKeys.value, valueToString && item[srcMapKeys.value] !== null && item[srcMapKeys.value] !== undefined ? item[srcMapKeys.value].toString() : item[srcMapKeys.value]), _objectSpread2), extands);

    if (children) {
      newItem[distMapKeys.children] = turnMapKeys(children, srcMapKeys, distMapKeys, valueToString, includesSourceItem);
    }

    return newItem;
  }) : [];
}
/**
 * @description:
 * @param tree {array}
 * @param value {string|array|number}
 * @param toDist {object}  默认 ： { src: "value", dist: "label" }  意思》将value值取tree对应的 label
 * @return: newValueArr
 */


function turnLabelOrValue(tree, value) {
  var toDist = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {
    src: "value",
    dist: "label"
  };
  var parValueArr = arguments.length > 3 ? arguments[3] : undefined;
  var newValueArr = parValueArr || [];

  if (Array.isArray(tree)) {
    tree.forEach(function (item) {
      if (Array.isArray(value)) {
        var index = value.indexOf(item[toDist.src]);

        if (index > -1 && !newValueArr[index]) {
          newValueArr[index] = item[toDist.dist];
        }
      } else if (value === item[toDist.src]) {
        newValueArr[0] = item[toDist.dist];
      }

      if (item.children) {
        turnLabelOrValue(item.children, value, toDist, newValueArr);
      }
    });
  }

  return Array.isArray(value) ? newValueArr : newValueArr[0];
}

var zTool = {
  getStyle: getStyle,
  setStyle: setStyle,
  hasClass: hasClass,
  addClass: addClass,
  removeClass: removeClass,
  on: on,
  off: off,
  once: once,
  dataTypeTest: dataTypeTest,
  deepCopyObject: deepCopyObject,
  deepCopyArray: deepCopyArray,
  deepCopy: deepCopy,
  formatDate: formatDate,
  GenNonDuplicateID: GenNonDuplicateID,
  arrayFilterBy: arrayFilterBy,
  EetoString: EetoString,
  onNoRepeatEvent: onNoRepeatEvent,
  filterQuery: filterQuery,
  parseQueryString: parseQueryString,
  formatterMapKey: formatterMapKey,
  isUrl: isUrl,
  checkDevices: checkDevices,
  loadFileList: loadFileList,
  dataType: dataType,
  firstWordToUpperCase: firstWordToUpperCase,
  removeItemFromTree: removeItemFromTree,
  replaceItemFromTree: replaceItemFromTree,
  pushItemToTree: pushItemToTree,
  itemsFromTree: itemsFromTree,
  isWhiteColor: isWhiteColor,
  turnMapKeys: turnMapKeys,
  turnLabelOrValue: turnLabelOrValue
};
exports.zTool = zTool;
var _default = zTool;
exports.default = _default;
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__("yLpj")))

/***/ }),

/***/ "mtxb":
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.ResizeSensor = void 0;

var ResizeSensor = function () {
  // Make sure it does not throw in a SSR (Server Side Rendering) situation
  if (typeof window === "undefined") {
    return null;
  } // Only used for the dirty checking, so the event callback count is limited to max 1 call per fps per sensor.
  // In combination with the event based resize sensor this saves cpu time, because the sensor is too fast and
  // would generate too many unnecessary events.


  var requestAnimationFrame = window.requestAnimationFrame || window.mozRequestAnimationFrame || window.webkitRequestAnimationFrame || function (fn) {
    return window.setTimeout(fn, 20);
  };
  /**
   * Iterate over each of the provided element(s).
   *
   * @param {HTMLElement|HTMLElement[]} elements
   * @param {Function}                  callback
   */


  function forEachElement(elements, callback) {
    var elementsType = Object.prototype.toString.call(elements);
    var isCollectionTyped = '[object Array]' === elementsType || '[object NodeList]' === elementsType || '[object HTMLCollection]' === elementsType || '[object Object]' === elementsType || 'undefined' !== typeof jQuery && elements instanceof jQuery //jquery
    || 'undefined' !== typeof Elements && elements instanceof Elements //mootools
    ;
    var i = 0,
        j = elements.length;

    if (isCollectionTyped) {
      for (; i < j; i++) {
        callback(elements[i]);
      }
    } else {
      callback(elements);
    }
  }
  /**
  * Get element size
  * @param {HTMLElement} element
  * @returns {Object} {width, height}
  */


  function getElementSize(element) {
    if (!element.getBoundingClientRect) {
      return {
        width: element.offsetWidth,
        height: element.offsetHeight
      };
    }

    var rect = element.getBoundingClientRect();
    return {
      width: Math.round(rect.width),
      height: Math.round(rect.height)
    };
  }
  /**
   * Class for dimension change detection.
   *
   * @param {Element|Element[]|Elements|jQuery} element
   * @param {Function} callback
   *
   * @constructor
   */


  var ResizeSensor = function ResizeSensor(element, callback) {
    /**
     *
     * @constructor
     */
    function EventQueue() {
      var q = [];

      this.add = function (ev) {
        q.push(ev);
      };

      var i, j;

      this.call = function () {
        for (i = 0, j = q.length; i < j; i++) {
          q[i].call();
        }
      };

      this.remove = function (ev) {
        var newQueue = [];

        for (i = 0, j = q.length; i < j; i++) {
          if (q[i] !== ev) newQueue.push(q[i]);
        }

        q = newQueue;
      };

      this.length = function () {
        return q.length;
      };
    }
    /**
     *
     * @param {HTMLElement} element
     * @param {Function}    resized
     */


    function attachResizeEvent(element, resized) {
      if (!element) return;

      if (element.resizedAttached) {
        element.resizedAttached.add(resized);
        return;
      }

      element.resizedAttached = new EventQueue();
      element.resizedAttached.add(resized);
      element.resizeSensor = document.createElement('div');
      element.resizeSensor.dir = 'ltr';
      element.resizeSensor.className = 'resize-sensor';
      var style = 'position: absolute; left: -10px; top: -10px; right: 0; bottom: 0; overflow: hidden; z-index: -1; visibility: hidden;';
      var styleChild = 'position: absolute; left: 0; top: 0; transition: 0s;';
      element.resizeSensor.style.cssText = style;
      element.resizeSensor.innerHTML = '<div class="resize-sensor-expand" style="' + style + '">' + '<div style="' + styleChild + '"></div>' + '</div>' + '<div class="resize-sensor-shrink" style="' + style + '">' + '<div style="' + styleChild + ' width: 200%; height: 200%"></div>' + '</div>';
      element.appendChild(element.resizeSensor);
      var position = window.getComputedStyle(element).getPropertyValue('position');

      if ('absolute' !== position && 'relative' !== position && 'fixed' !== position) {
        element.style.position = 'relative';
      }

      var expand = element.resizeSensor.childNodes[0];
      var expandChild = expand.childNodes[0];
      var shrink = element.resizeSensor.childNodes[1];
      var dirty, rafId, newWidth, newHeight;
      var size = getElementSize(element);
      var lastWidth = size.width;
      var lastHeight = size.height;

      var reset = function reset() {
        //set display to block, necessary otherwise hidden elements won't ever work
        var invisible = element.offsetWidth === 0 && element.offsetHeight === 0;

        if (invisible) {
          var saveDisplay = element.style.display;
          element.style.display = 'block';
        }

        expandChild.style.width = '100000px';
        expandChild.style.height = '100000px';
        expand.scrollLeft = 100000;
        expand.scrollTop = 100000;
        shrink.scrollLeft = 100000;
        shrink.scrollTop = 100000;

        if (invisible) {
          element.style.display = saveDisplay;
        }
      };

      element.resizeSensor.resetSensor = reset;

      var onResized = function onResized() {
        rafId = 0;
        if (!dirty) return;
        lastWidth = newWidth;
        lastHeight = newHeight;

        if (element.resizedAttached) {
          element.resizedAttached.call();
        }
      };

      var onScroll = function onScroll() {
        var size = getElementSize(element);
        var newWidth = size.width;
        var newHeight = size.height;
        dirty = newWidth != lastWidth || newHeight != lastHeight;

        if (dirty && !rafId) {
          rafId = requestAnimationFrame(onResized);
        }

        reset();
      };

      var addEvent = function addEvent(el, name, cb) {
        if (el.attachEvent) {
          el.attachEvent('on' + name, cb);
        } else {
          el.addEventListener(name, cb);
        }
      };

      addEvent(expand, 'scroll', onScroll);
      addEvent(shrink, 'scroll', onScroll); // Fix for custom Elements

      requestAnimationFrame(reset);
    }

    forEachElement(element, function (elem) {
      attachResizeEvent(elem, callback);
    });

    this.detach = function (ev) {
      ResizeSensor.detach(element, ev);
    };

    this.reset = function () {
      element.resizeSensor.resetSensor();
    };
  };

  ResizeSensor.reset = function (element, ev) {
    forEachElement(element, function (elem) {
      elem.resizeSensor.resetSensor();
    });
  };

  ResizeSensor.detach = function (element, ev) {
    forEachElement(element, function (elem) {
      if (!elem) return;

      if (elem.resizedAttached && typeof ev === "function") {
        elem.resizedAttached.remove(ev);
        if (elem.resizedAttached.length()) return;
      }

      if (elem.resizeSensor) {
        if (elem.contains(elem.resizeSensor)) {
          elem.removeChild(elem.resizeSensor);
        }

        delete elem.resizeSensor;
        delete elem.resizedAttached;
      }
    });
  };

  return ResizeSensor;
}();

exports.ResizeSensor = ResizeSensor;
var _default = ResizeSensor;
exports.default = _default;

/***/ })

}]);
//# sourceMappingURL=5.4ebfda1f88e76a2e3e19.js.map